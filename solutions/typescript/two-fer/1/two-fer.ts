/**
 * This stub is provided to make it straightforward to get started.
 */

export function twoFer(otherPerson?: string): string {
  if (otherPerson === undefined) otherPerson = 'you';
  return `One for ${otherPerson}, one for me.`
}
